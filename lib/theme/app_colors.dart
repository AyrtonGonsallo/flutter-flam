import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFFE50011);  // rouge
  static const secondary = Color(0xFFD8BF6C); // or
  static const dark = Color(0xFF333333);      // gris très foncé
  static const light = Color(0xFFF8F8F8);     // fond clair
  static const white = Color(0xFFFFFFFF);     // fond clair
  static const black = Color(0xFF000000);     // fond clair
}